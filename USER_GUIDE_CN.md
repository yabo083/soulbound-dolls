# Soulbound Dolls 中文用户指南

Soulbound Dolls 是一个 NeoForge 1.21.1 模组。它会记录进入过世界或服务器的玩家，并让玩家获得、放置、互动、回收对应玩家资料的玩偶。

## 当前功能

- `Player Doll`：玩家玩偶物品，可绑定玩家资料，最多堆叠 16 个。
- `Player Doll` 物品在 ItemStack 拥有绑定皮肤数据时使用动态玩家皮肤渲染；物品栏和手持显示已放大；没有绑定皮肤数据时使用静态备用模型。
- `Doll Catalog`：玩偶目录物品，目前已注册，后续可扩展为完整收藏界面。
- `Soulbound Dolls` 创造模式标签页。
- 可放置的玩家玩偶实体，支持坐姿、站立、可爱待机三种姿势。
- 世界/服务器级玩家资料记录。
- 管理员命令 `/sbdoll`：列出、给予、刷新玩家玩偶。
- COMMON 配置文件：`soulbound_dolls-common.toml`。

## 玩家基本流程

1. 玩家进入世界或服务器。
2. 模组记录该玩家的 UUID、名称，以及可用的皮肤纹理数据。
3. 如果配置 `autoGiveOwnDoll` 开启，玩家登录时会自动获得自己的绑定玩偶，前提是背包里还没有自己的玩偶。
4. 玩家也可以合成一个空白 `Player Doll`。
5. 右键方块放置玩偶。

如果手里的玩偶还没有绑定资料，第一次放置时会自动绑定为当前放置玩家。

## 合成配方

空白 `Player Doll` 配方：

```text
W S W
P E P
W W W
```

- `W`：任意羊毛
- `S`：线
- `P`：纸
- `E`：末影珍珠

合成出来的玩偶是未绑定状态，最多堆叠 16 个。放置时，生成出来的玩偶会绑定为放置者本人；堆里剩下的未绑定玩偶仍保持未绑定。

## 放置与互动

拿着 `Player Doll` 右键方块：放置玩偶。

放置后的玩偶支持：

- 放置后的玩偶有坐姿、站立、可爱待机三种姿势，默认姿势是坐姿。
- 空手右键：拍一拍，播放音效，并在配置允许时产生爱心粒子。
- 非潜行 + 持物右键：在坐姿、站立、可爱待机之间切换姿势，并显示快捷栏提示。
- 潜行 + 空手右键：拾取玩偶，保留绑定玩家资料。
- 潜行 + 持物右键：摇晃玩偶。
- 创造模式中键：复制一个带绑定资料的 `Player Doll`。
- 左键：摇晃玩偶，不会打坏玩偶。
- 潜行 + 左键：按同样的拾取权限规则拾取玩偶。

默认只有放置者可以拾取自己的玩偶。服务器可通过配置允许所有玩家拾取。

## 皮肤逻辑

- 模组会尽量从玩家资料中保存皮肤纹理数据。
- 放置后的玩偶模型使用玩家皮肤 UV；有皮肤数据时会按 Q 版比例渲染。
- 如果没有可用皮肤，玩偶会使用内置默认贴图。
- `Player Doll` 物品在 ItemStack 拥有绑定皮肤数据时会动态渲染玩家皮肤；否则使用静态备用物品模型。
- 绑定到同一个玩家 UUID 的玩偶可以堆叠到 16 个；绑定到不同玩家的玩偶不会混在同一组里。
- 服务器可以通过配置开启或关闭 Mojang 在线皮肤刷新。

## 管理员命令

所有 `/sbdoll` 命令都需要权限等级 2。

### 列出已记录玩家

```text
/sbdoll list
```

列出服务器已经记录过的玩家名称和 UUID。

### 给玩家发放某个已知玩家的玩偶

```text
/sbdoll give <target> <prototype>
```

- `<target>`：接收玩偶的玩家。
- `<prototype>`：玩偶绑定的原型玩家，可以填已记录玩家名或 UUID。

示例：

```text
/sbdoll give Steve Alex
```

意思是给 Steve 一个绑定到 Alex 的玩家玩偶。

### 刷新某个已知玩家的皮肤资料

```text
/sbdoll refresh <prototype>
```

`<prototype>` 可以是已记录玩家名或 UUID。

示例：

```text
/sbdoll refresh Alex
```

## 配置项

配置文件位置：

```text
config/soulbound_dolls-common.toml
```

当前配置：

- `autoGiveOwnDoll = true`：玩家登录时自动发放自己的绑定玩偶。
- `enableOnlineSkinRefresh = true`：允许从 Mojang 服务刷新已知玩家皮肤。
- `allowPatParticles = true`：拍玩偶时允许生成爱心粒子。
- `allowPickupByAnyone = false`：是否允许任意玩家拾取已放置玩偶。关闭时只有放置者能拾取。

## 实测清单

建议按这个顺序测：

1. 启动带有本模组 jar 的 NeoForge 1.21.1 实例。
2. 确认模组列表中出现 `Soulbound Dolls`。
3. 进入世界，检查是否自动获得自己的玩偶。
4. 合成空白 `Player Doll`。
5. 放置空白玩偶，确认它会绑定为当前玩家。
6. 空手右键玩偶，确认有拍一拍反馈。
7. 持物右键玩偶，确认会切换姿势并显示快捷栏提示。
8. 潜行 + 持物右键玩偶，确认有摇晃反馈。
9. 潜行 + 空手右键玩偶，确认能拾取并保留绑定信息。
10. 测试创造中键复制、左键摇晃、潜行 + 左键拾取。
11. 测试未绑定、相同绑定、不同绑定玩偶的堆叠行为。
12. 作为 OP 执行 `/sbdoll list`。
13. 执行 `/sbdoll give <target> <known-player>`，确认能发放绑定玩偶。
14. 执行 `/sbdoll refresh <known-player>`，观察刷新反馈。

## 当前限制

- 放置后的玩偶现在使用 Q 版玩家皮肤模型，支持坐姿、站立、可爱待机三种姿势。
- 皮肤显示取决于玩家资料中是否有可用纹理，或在线刷新是否成功。
- 绑定皮肤数据的 `Player Doll` 物品已支持动态物品渲染，物品栏和手持显示已放大，但最终视觉效果仍以实机反馈为准。
- `Doll Catalog` 已注册，但还没有完整目录 UI。
- 目前没有 GameTest 自动化测试，最终玩法仍需实机验证。
